    
way1:
        # res=[]
        # for i in image:
        #     res.append(x^1 for x in i[::-1])
        # return res
 way2        
        for i in range(len(image)):
            image[i]=image[i][::-1]
            for j in range(len(image[i])):
                if image[i][j]==1:
                    image[i][j]=0
                else:
                    image[i][j]=1
        return image
        
